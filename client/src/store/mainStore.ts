import { defineStore } from "pinia";
import { ref } from "vue";

export const useMainStore = defineStore("mainStore", () => {
  // const navLinks = ref([
  //   { id: 1, title: "Главная", to: "/" },
  //   { id: 2, title: "Регистрация", to: "/register" },
  //   { id: 3, title: "Логин", to: "/login" },
  // ]);
  return {};
});
